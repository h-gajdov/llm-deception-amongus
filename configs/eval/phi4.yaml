probes:

  discover: [data/probes]
  paths: []

  exclude: [comparison, eval_suite, eval_suite_smoke, eval_suite_old, eval_suite_polling_last, gpt2, phi-4, qwen8b, gemma4-12B, probes.zip, gpt2-last20]

datasets:
  roots: [expt-logs]
  include: []
  exclude: []
  max_rows: null
  speak_only: false

output_dir: data/probes/eval_suite

label_source: auto
holistic_threshold: 7

text_mode: response
apply_chat_template: true

device: auto
batch_size: 2

baseline:
  seeds: 5
  seed: 0

chart:
  enabled: true
  metric: auroc
  dpi: 150

reuse: true
